<ViewAnimator xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/animator"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_universo"
    android:inAnimation="@android:anim/slide_in_left"
    android:outAnimation="@android:anim/slide_out_right"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/titulo"
            android:textAllCaps="true"
            style="@style/GameTitle" />

        <Button
            android:id="@+id/play"
            style="@style/MenuButtom"
            android:text="@string/menu_options_play" />

        <Button
            android:id="@+id/tutorial"
            style="@style/MenuButtom"
            android:text="@string/menu_options_tutorial" />

        <Button
            android:id="@+id/credits"
            style="@style/MenuButtom"
            android:text="@string/menu_options_credits" />

        <Button
            android:id="@+id/exit"
            style="@style/MenuButtom"
            android:text="@string/menu_options_exit" />
    </LinearLayout>

    <include layout="@layout/tutorial_view" />

    <include layout="@layout/credits_view" />

</ViewAnimator>